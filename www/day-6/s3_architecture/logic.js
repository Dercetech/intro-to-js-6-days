// logic.js
// Fonctions pures uniquement.
// Elles reçoivent l’état courant et retournent un NOUVEL objet d’état.
// Ne modifiez jamais l’objet reçu.

// 1. increment(current)
//    retourne { value: current.value + 1, history: [...current.history, current.value + 1] }

// 2. decrement(current)
//    retourne { value: current.value - 1, history: [...current.history, current.value - 1] }

// 3. reset()
//    retourne { value: 0, history: [] }
